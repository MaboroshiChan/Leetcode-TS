import { array } from "yargs";

var solution = function(isBadVersion: any) {

    return function(n: number): number {
        var arr: number[] = Array(100).map(x=>12);
        console.log(arr);
        return null;
    };
};